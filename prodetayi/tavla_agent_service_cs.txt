`TavlaAgentService.cs`, `tavlajules` adli dakikalik orkestrator ajanidir.

- `jules remote list --session` ile Jules durumlarini okur.
- Jules session listesini sadece `kaanx2311336/julestavla` veya izlenen session ID ile ilgili satirlara filtreler; eski Batak sessionlari modele verilmez.
- Izlenen session tamamlanmis gorunurse `jules remote pull --session <id>` ile sonucu ceker ama patch'i uygulamaz.
- Aiven `ajanlarim` DB saglik durumunu okur.
- `prodetayi/` ve `yapilanlar/` hafizasini toplayip OpenRouter ajanina verir.
- OpenRouter sistem promptu, Batak projesine benzer fazli/prodetayi/yapilanlar disiplinini koruyacak sekilde tasarlandi.
- Prompt 2026-04-24 guncellemesiyle daraltildi: Batak yalnizca surec disiplini ornegi, cevap icerigi kesinlikle TavlaJules/tavla olacak.
- Modelden JSON bekler: durum ozeti, Jules'in yaptiklari, sonraki prompt, otomatik session onayi, DB plani ve risk notlari.
- OpenRouter free modelleri 503/429 verirse tur basarisiz sayilmaz; `local-degraded` rapor uretilir ve SQL'e yazilir.
- Her turu `agent_reports/YYYY_MM_DD_HH_mm_ss_agent_report.json` dosyasina yazar.
- Her turu `AgentSqlReporter` ile Aiven `ajanlarim` DB icindeki `agent_runs`, `agent_events` ve `agent_jules_sessions` tablolarina yazar.
- Her turdan sonra `AgentDashboardExporter` ile `ajanizleme/data/dashboard.json` ve `dashboard-data.js` dosyalarini gunceller.
- `AllowAutoJulesSessions` true ise ve model de onay verirse yeni Jules session baslatabilir; varsayilan guvenli davranis yalnizca onermektir.
- `jules remote list --session` ciktisinda `Awaiting User Feedback/Input` yakalanirsa bunu normal Planning degil, Jules'in soru sordugu durum olarak ayri raporlar.
- 2026-04-25 duzeltmesiyle Jules CLI status sutunu bos gelse bile satir eski/inaktif gorunuyor veya aciklama "please provide/exact method/clarification" gibi kullanici cevabi istiyorsa `Awaiting Input` kabul edilir.
- `AutoReplyAwaitingInputSessions` true ise repo icindeki `Awaiting User Feedback` sessionlarina `JulesCliService.ReplyToSessionAsync` ile net GameEngine metot cevabi gonderir.
- Jules `Awaiting Plan Approval` durumuna gecer ise ayni interact akisiyle plan onayi gonderir ve session `In Progress` olana kadar takip eder.
- Ayni session'a ayni cevabin dakika basi tekrar gitmemesi icin `RepliedAwaitingInputSessionIds` ve `ApprovedAwaitingPlanSessionIds` kullanilir.
- API reply basarisiz olursa eski `AutoRecoverAwaitingInputSessions` yedek davranisi korunur; ancak basarili reply varsa yeni kopya session acilmaz.
- 2026-04-24 devam duzeltmesiyle `AutoContinueCompletedSessions` true ise izlenen Jules session `Completed` veya `Needs review` oldugunda model onayi beklemeden tek seferlik devam session'i acar.
- Devam edilen completed session id'leri `AgentStateService` ile `agent_state/tavlajules.json` icinde tutulur; bu sayede ayni completed session icin dakika basi tekrar tekrar yeni Jules gorevi acilmaz.
- Yeni Jules session id cikti icinden okunabilirse `settings.TrackedJulesSessionId` yeni id ile guncellenir ve sonraki turlar artik onu izler.
- Tam otonom modda completed session icin once git calisma alani temizligi kontrol edilir, sonra `jules remote pull --session <id> --apply` ile patch uygulanir.
- Apply basarili olursa `dotnet build` ve `dotnet test` calistirilir; dogrulama basariliyse staged secret taramasi yapilip commit/push denenir.
- Apply, dogrulama, secret scan, commit veya push bloklanirsa yeni Jules session acilmaz; hata `agent_events`, JSON rapor ve dashboard'a yazilir.
- OpenRouter baglamina son agent raporlari, otonom komut ozetleri ve anlik Jules/Git durumu eklenir; model sonraki promptu bu hafizayla tasarlar.
- 2026-04-24 tekrar duzeltmesiyle `jules remote list` icindeki kisaltilmis repo adlari da yakalanir; ayni aciklamaya sahip completed session daha once islenen/applied session ile eslesirse tekrar apply edilmez.
- OpenRouter kullanilamazsa `local-degraded` artik yeni Jules promptu uretmez; sadece SQL/dashboard raporu yazar. Boylece ayni prompt free model hatalarinda tekrar tekrar Jules'e gitmez.
- Gonderilen nextPrompt SHA-256 parmak izi `AgentStateService` icinde tutulur; ayni prompt tekrar gelirse yeni session acilmaz.
- 2026-04-25 iyilestirmesiyle prompt sadece hash ile degil hedef anahtariyla da takip edilir; `GenerateLegalMoves` gibi ayni hedef farkli cumleyle gelirse yeni Jules session acilmaz veya sonraki oyun fazina planlanir.
- OpenRouter `GenerateLegalMoves` gibi mevcut/denenmis hedefi tekrar onerirse ajan deterministik yol haritasina bakar: explicit dice legal moves -> full-turn move sequences -> game state snapshot sirasi.
- Completed bir Jules session'in hedefi mevcut lokal kodda zaten uygulanmis gorunurse `pull --apply` atlanir; bu ozellikle eski tekrar session'larinin ayni patch'i tekrar uygulamasini engeller.
- 2026-04-25 devam duzeltmesiyle hedef zaten uygulanmis completed session'da OpenRouter `nextPrompt:null` verse bile ajan lokal yol haritasindan sonraki faz promptunu secip yeni tracked Jules session'i acar.
- Hedef siniflandirma sirasinda `move sequence/full-turn` ifadeleri `GenerateLegalMoves` referansindan once yakalanir; boylece yeni full-turn fazi yanlislikla eski legal-move hedefi sayilmaz.
- BuildProjectContext icinde `OYUN FAZ DURUMU` vardir; model done gorunen hedefi tekrar onermemelidir.
- 2026-04-25 plan/input duzeltmesiyle Jules session basligi `Awaiting User Feedback` gorunse bile aciklama `please approve`, `plan so Jules can proceed`, `already implemented`, `finalize this commit` gibi onay/finalize cumleleri iceriyorsa input degil plan onayi olarak islenir.
- Awaiting input cevaplari artik tek bir hardcoded GameEngine dice/turn sablonu degildir; session aciklamasina gore snapshot, legal move/turn veya genel netlestirme promptu secilir.
- Snapshot netlestirme cevabi `public static GameStateSnapshot CaptureGameStateSnapshot(GameEngine engine)` imzasini sabitler; mevcut capability zaten varsa Jules'e ek kod yazmadan session'i complete/finalize etmesi soylenir.
- OpenRouter sistem promptu `ajanlarim` ve `tavla_online` icin Aiven MySQL gercegini vurgular; PostgreSQL/JSONB/EF Core DB planlari ancak repo zaten o stack'i acikca kullaniyorsa onerilebilir.
- 2026-04-25 retry duzeltmesiyle `Awaiting Plan Approval` icin onay daha once gonderilmis olsa bile session hala bekliyorsa 10 dakikalik aralikla en fazla 3 kez tekrar denenir.
- `Awaiting Plan Approval` durumundaki izlenen session'in hedefi lokal kodda zaten uygulanmis gorunurse ajan bunu no-op completed kabul eder; ayni promptu tekrar Jules'e gondermez ve paneli bekleme modunda dondurmaz.
- `exact method signature` ve `CaptureGameStateSnapshot` ifadeleri `engine.game-state-snapshot` hedefi olarak siniflandirilir; kisaltilmis Jules satirlarinda snapshot tekrar promptlari azaltilir.
- 2026-04-25 continuation routing ile completed Jules session sonrasi nextPrompt ayni engine zincirine bagliysa `ReplyToSessionAsync` ile mevcut session'in altina yazilir; DB/UI/dashboard gibi ayri yuzeylere geciyorsa yeni Jules session acilir.
- Ayni session altinda devam edilirse state icindeki eski completed/applied kaydi temizlenir ve session-objective map guncellenir; boylece ayni session yeni is tamamladiginda tekrar apply/dogrulama akisi bloke olmaz.
- 2026-04-25 takilma duzeltmesiyle `engine.move-sequences` hedefi ayri `MoveSequenceGenerator.cs` dosyasi olmasa da `GameEngine.GenerateLegalMoveSequences`, `GenerateSequencesRecursive` ve maksimum zar kullanim kurali varsa tamamlanmis sayilir.
- OpenRouter zaten uygulanmis veya Jules listesinde bulunan hedefi tekrar onerir ve lokal roadmap uygun yeni faz bulamazsa ajan eski promptu gondermez; `next_prompt_already_done_skipped` event'i yazar ve donguyu bekleme/raporlama modunda tutar.
- Hedefi zaten uygulanmis completed/no-op session sonrasinda sonraki faz secilirken kisaltilmis Jules basligi yerine state icindeki session-objective map okunur; boylece snapshot gibi kisaltilmis basliklar yanlis hedefe dusmez.
- Engine fazlari tamamlaninca deterministik roadmap artik `data.mysql-game-persistence` fazina gecer; bu faz `migrations/`, `GameStateRepository`, MySQL uyumlu `games`, `game_state_snapshots`, `move_sequences`, `dice_rolls` tablolarini hedefler.
- `data.mysql-game-persistence` engine disi bir yuzey sayilir; completed engine session altina yazilmaz, yeni Jules session olarak acilir.
- 2026-04-25 DB baslatma duzeltmesiyle prompt hedef siniflandirmasinda MySQL/tavla_online/persistence anahtarlari `move sequence` anahtarlarindan once yakalanir; DB promptlari yanlislikla `engine.move-sequences` sayilip atlanmaz.
- OpenRouter DB fazini `src/TavlaJules.Persistence`, Docker veya repo disi genis bir yapiyla onerirse ajan promptu mevcut repo roadmap'ine kanoniklestirir: `src/TavlaJules.Data`, raw MySQL migration, mevcut test projesi ve prodetayi/yapilanlar guncellemesi.
- Completed DB/persistence Jules patch'i sadece `TavlaAgentService.cs` gibi meta/orkestrator dosyalarina dokunursa yanlis hedef sayilir; patch uygulanmaz, session handled isaretlenir, prompt/objective tekrar korumasi temizlenir ve ayni hedef dogru data/migration/test dosyalariyla yeniden gorevlendirilebilir.
- `LoadSnapshotAsync` / load snapshot isleri ayri `data.mysql-load-snapshot` hedefi olarak siniflandirilir; onceki genis persistence session'i duplicate sayilip bu daha dar is engellenmez.
- Load snapshot promptlari `GameStateRepository`, mevcut test projesi, prodetayi/yapilanlar ile sinirlanir; yeni proje, EF Core, Docker veya `TavlaAgentService` degisikligi yasaklanir.
- 2026-04-25 duplicate duzeltmesiyle `SessionsContainObjective` completed/needs-review/archived Jules satirlarini yeni prompt icin aktif duplicate saymaz; yalnizca Planning/In Progress/Awaiting gibi devam eden isler ayni hedefi bloke eder.
- `BuildPromptObjectiveKey` icinde full MySQL persistence sinyalleri (`IGameRepository`, `move_sequences`, `dice_rolls`, online persistence phase vb.) load-snapshot sinyallerinden once yakalanir; `payloads` gibi icinde `load` gecen alakasiz kelimeler artik `data.mysql-load-snapshot` yanlis pozitifine neden olmaz.
- 2026-04-26 tam otonom apply duzeltmesiyle duplicate completed session kontrolu artik `jules remote pull --session <id>` on izlemesinden sonra calisir; yeni session gercek `diff --git` patch'i tasiyorsa ve eslesen eski session apply edilmemisse duplicate diye durmaz, apply/build/test/commit akisi devam eder.
- Dirty workspace recovery basarili olursa otomasyon artik sonda tekrar `blocked=true` yapmaz; onceki apply dogrulanip commit/push edildiginde dongu toparlanmis sayilir.
- 2026-04-26 otonom roadmap duzeltmesiyle MySQL persistence tek hedef olmaktan cikarildi: `data.mysql-schema` migration/MySqlConnector asamasini, `data.mysql-repository` ise gercek async repository metotlarini temsil eder. Sadece migration gelmesi artik repository fazini tamamlanmis saydirmaz.
- Eski `data.mysql-game-persistence` hedefi geriye uyumlu bir ust hedef olarak tutulur ama tamamlanmis sayilmasi icin hem MySQL semasi hem de `SaveSnapshotAsync`, `LoadSnapshotAsync`, `SaveMoveSequenceAsync`, `SaveDiceRollAsync` ve ilgili MySQL tablo referanslarini iceren repository katmani gerekir.
- Completed MySQL schema/session ardindan sonraki prompt repository katmanina bagli kabul edilir; mumkunse ayni completed Jules session altina yazilir, ancak hedef anahtari `data.mysql-repository` olarak ayrildigi icin eski genis persistence prompt kayitlari yeni isi susturmaz.
