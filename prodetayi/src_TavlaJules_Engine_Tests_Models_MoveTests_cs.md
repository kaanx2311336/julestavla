# src/TavlaJules.Engine.Tests/Models/MoveTests.cs

Unit tests for the `Move` class.
Verifies that properties are set correctly via the constructor and checks the default values for `IsHit` and `DiceUsed`.
